# dependencies
node_modules
.pnp
.pnp.*

# next.js
.next
out
build

# production
/backups

# environment
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# misc
.DS_Store
*.pem
coverage
.vercel

# typescript
next-env.d.ts
*.tsbuildinfo

# generated
/lib/generated

# local agent tooling
.atl

# per-machine docker overrides (e.g. forcing build network: host when the local
# Docker bridge can't reach the registry). Kept local — must not affect the VPS.
docker-compose.override.yml
docker-compose.override.yaml
